// Semantics pass — stub.
//
// The real analyser (symbol resolution, type checking, borrow
// checking) is tracked on the v0.2 roadmap. The .cpp exists so the
// build system can target the directory and so a future change has a
// place to land without adding files at the build-system level.
#include "semantics.hpp"
