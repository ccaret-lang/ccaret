// Translation-unit placeholder. The header is a constant-only header
// today; we keep the .cpp around so adding a future helper (e.g.
// canonical-name normalisation) does not force a header-only
// inclusion change in the build system.
#include "syntax.hpp"
